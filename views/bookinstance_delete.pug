extends layout

block content
  h1 #{title}: #{book_instance.imprint} of book #{book_instance.book.title}

  p Do you really want to delete this book instance?

  form(method='POST' action='')

    div.form-group
      input#book_instanceid.form-control(type='hidden' name='book_instanceid' value=book_instance._id required='true')
      button.btn.btn-primary(type='submit') Delete
